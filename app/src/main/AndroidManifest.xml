<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chandrachud.bubble">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name="com.chandrachud.bubble.Services.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.chandrachud.bubble.activities.AddGoalActivity" />
        <activity android:name="com.chandrachud.bubble.activities.GoalCenter" />
        <activity android:name="com.chandrachud.bubble.activities.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.chandrachud.bubble.activities.MainActivity" />
        <activity android:name="com.chandrachud.bubble.activities.GoalsActivity" />
        <activity android:name="com.chandrachud.bubble.activities.timelineActivity" />
        <activity android:name="com.chandrachud.bubble.activities.AppInfoActivity" />
        <activity android:name="com.chandrachud.bubble.activities.TimerActivity" />
        <activity
            android:name="com.chandrachud.bubble.activities.YourAppsActivity"
            android:label="@string/title_activity_timer"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <service android:name="com.chandrachud.bubble.Services.DetectAppService" />
    </application>

</manifest>